name: Build Newifi3 OpenWrt

on:
  workflow_dispatch: # 仅允许手动点击 Run workflow 触发，避免不必要的占用

permissions:
  contents: write    # 解决 403 Release 报错
  actions: write

env:
  REPO_URL: https://github.com/coolsnowwolf/lede
  REPO_BRANCH: master
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true # 解决 Node 版本过旧警告

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout GitHub Actions Scripts
      uses: actions/checkout@v4

    - name: Initialize Environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp nano ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pip libpython3-dev qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"

    - name: Clone Lean's OpenWrt Source Code
      run: |
        git clone $REPO_URL -b $REPO_BRANCH openwrt

    - name: Clone 3rd-party Plugins (Diskman)
      run: |
        cd openwrt
        mkdir -p package/custom
        # 单独拉取大名鼎鼎的 Diskman 插件
        git clone https://github.com/lisaac/luci-app-diskman.git package/custom/luci-app-diskman

    - name: Update & Install Feeds
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: Inject Custom Configuration (Newifi3 D2)
      run: |
        cd openwrt
        rm -f .config
        touch .config
        
        cat >> .config <<EOF
        # 1. 硬件锁定
        CONFIG_TARGET_ramips=y
        CONFIG_TARGET_ramips_mt7621=y
        CONFIG_TARGET_ramips_mt7621_DEVICE_d-team_newifi-d2=y
        
        # 2. 核心网络需求与服务
        CONFIG_PACKAGE_luci-app-wireguard=y
        CONFIG_PACKAGE_luci-app-openvpn=y
        CONFIG_PACKAGE_luci-app-openvpn-server=y
        CONFIG_PACKAGE_luci-app-aliddns=y
        CONFIG_PACKAGE_luci-app-opkg=y
        
        # 3. 实用小工具 (新增: TTYD, KMS, WOL, ARP绑定)
        CONFIG_PACKAGE_luci-app-ttyd=y
        CONFIG_PACKAGE_luci-app-vlmcsd=y
        CONFIG_PACKAGE_luci-app-wol=y
        CONFIG_PACKAGE_luci-app-arpbind=y
        
        # 4. AdGuardHome (仅UI，不带核心执行文件)
        CONFIG_PACKAGE_luci-app-adguardhome=y
        
        # 5. 磁盘管理 (Diskman & 依赖)
        CONFIG_PACKAGE_luci-app-diskman=y
        CONFIG_PACKAGE_parted=y
        CONFIG_PACKAGE_blkid=y
        CONFIG_PACKAGE_e2fsprogs=y
        
        # 6. U盘底层驱动与文件系统支持
        CONFIG_PACKAGE_kmod-usb-core=y
        CONFIG_PACKAGE_kmod-usb3=y
        CONFIG_PACKAGE_kmod-usb-storage=y
        CONFIG_PACKAGE_kmod-usb-storage-extras=y
        CONFIG_PACKAGE_kmod-fs-ext4=y
        CONFIG_PACKAGE_kmod-fs-vfat=y
        CONFIG_PACKAGE_kmod-nls-utf8=y
        CONFIG_PACKAGE_block-mount=y
        
        # 7. 安全减负 (强制剔除不需要的默认大体积插件)
        CONFIG_PACKAGE_luci-app-adbyby-plus=n
        CONFIG_PACKAGE_luci-app-unblockmusic=n
        CONFIG_PACKAGE_luci-app-zerotier=n
        CONFIG_PACKAGE_luci-app-ssr-plus=n
        CONFIG_PACKAGE_luci-app-ipsec-vpnd=n
        CONFIG_PACKAGE_luci-app-xlnetacc=n
        CONFIG_PACKAGE_luci-app-vsftpd=n
        EOF
        
        # 补全其余依赖
        make defconfig

    - name: Download Packages
      run: |
        cd openwrt
        make download -j8
        find dl -size -1024c -exec rm -f {} \;

    - name: Compile the Firmware
      id: compile
      run: |
        cd openwrt
        echo -e "$(nproc) thread compile"
        make -j$(nproc) || make -j1 || make -j1 V=s
        echo "status=success" >> $GITHUB_OUTPUT
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: Organize Files
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload Firmware to Release
      uses: softprops/action-gh-release@v2
      if: steps.organize.outputs.status == 'success' && env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        tag_name: "Newifi3-D2_${{ env.FILE_DATE }}"
        name: "Newifi3 D2 终极养老版 (全能辅助)"
        body: "专属定制：包含 WireGuard, OpenVPN, AliDDNS, Diskman, AGH控制面板。已新增 TTYD终端, KMS激活, WOL唤醒, ARP绑定。全面支持外挂 U 盘扩展空间。"
        files: ${{ env.FIRMWARE }}/*sysupgrade.bin